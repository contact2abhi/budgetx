function Spend(title, date, amount, categroryId, id){
    this.title = title,
    this.date = date,
    this.amount = amount,
    this.categroryId = categroryId,
    this.id = id
}

export default Spend;